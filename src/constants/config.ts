// Default backend/laptop IP address used by the mobile app.
export const DEFAULT_SERVER_IP = '192.168.1.5';

// Default latitude value used as fallback/test location.
export const DEFAULT_LATITUDE = '26.1445';

// Default longitude value used as fallback/test location.
export const DEFAULT_LONGITUDE = '91.7362';

// Device name sent to backend during device registration.
export const DEVICE_NAME = 'Android Phone';

// Number of emergency photos to capture from each camera.
export const DANGER_IMAGE_COUNT_PER_CAMERA = 5;

// Emergency audio recording duration in seconds.
export const DANGER_AUDIO_RECORD_SECONDS = 30;

// Audio MIME type sent to backend.
export const DANGER_AUDIO_MIME_TYPE = 'audio/mp4';

// Audio file extension.
export const DANGER_AUDIO_EXTENSION = 'm4a';